# 同一portで server_name が異なる複数server（正常系）
server {
    listen 8080;
    server_name example.com;
    root /var/www/example;
}

server {
    listen 8080;
    server_name other.example.com;
    root /var/www/other;
}
