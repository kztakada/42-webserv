# 意地悪だけど正常: 同じpatternのlocationを複数定義（パーサーは拒否しない）
server {
    listen 8083;
    root /var/www;

    location /same {
        allow_methods GET;
    }

    location back /same {
        allow_methods GET;
    }
}
